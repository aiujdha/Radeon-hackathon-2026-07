# Collaboration Role Value Fix

## Goal

Allow administrators to add and update project members after role labels were
localized into Chinese.

## Acceptance criteria

- The UI displays localized role labels.
- The API receives only its accepted role enum values: `admin`, `pm`,
  `member`, or `guest`.
