# Web 工作台中文化技术设计

- Level: S2
- Status: implemented

## 实现

- 在 React 组件中替换用户可见静态文案和 ARIA 标签；
- 为任务状态、角色、风险生命周期、审批决策和运维健康状态提供显示层中文映射；
- 保持 DTO、请求参数、`value` 枚举、API 路径和服务端返回值不变；
- 继续以 TypeScript 检查、现有前端测试、生产构建和严格 Spec 校验验证。
