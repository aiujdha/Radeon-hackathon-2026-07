# UI-5 Admin Operations Visibility Fix

The system administrator operations summary must be discoverable immediately
after the project overview. It must not require a user to scroll through every
project work surface before confirming the deployment state.
